file = dglazing.data
grid = 18 x 18 x 18
format = ascii
interleaving = field
majority = column
header = lines 2
field = Flow, Temperature, Pressure
structure = 3-vector, scalar, scalar
type = float, float, float
dependency = positions, positions, positions
positions = regular, regular, regular, 0, 1, 0, 1, 0, 1

end
