file = brain.data
grid = 72 x 82
format = ascii
interleaving = record
majority = column
header = lines 1
field = BoneDensity, BloodFlow
structure = scalar, scalar
type = int, int
dependency = positions, positions
positions = regular, regular, 0, 1, 0, 1

end
